DEEPSEEK_API_KEY=your-deepseek-api-key-here
QDRANT_URL=https://your-cluster.qdrant.io
QDRANT_API_KEY=your-qdrant-api-key
QDRANT_COLLECTION=soap_notes
OPENAI_API_KEY=sk-...    # used only for Whisper transcription (~$0.006/min)

# Phase 4 — Auth (add to web/.env.local, not this file)
# AUTH_SECRET=<run: python -c "import secrets; print(secrets.token_hex(32))">
# NEXTAUTH_URL=http://localhost:3000
# ALLOWED_EMAILS=you@clinic.com,doctor@clinic.com   # comma-separated; leave empty to allow any email (dev)
